<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android" android:exitFadeDuration="@android:integer/config_mediumAnimTime">

    <!-- Pressed state - touch press or D-pad / physical keyboard click -->
    <item android:drawable="@color/default_black_selector_pressed" android:state_pressed="true"/>

    <!-- Selected/Focused state - D-pad / physical keyboard -->
    <item android:drawable="@color/default_black_selector_selected" android:state_selected="true"/>
    <item android:drawable="@color/default_black_selector_focused" android:state_focused="true"/>

    <!-- Normal state -->
    <item android:drawable="@color/default_black_selector"/>

</selector>