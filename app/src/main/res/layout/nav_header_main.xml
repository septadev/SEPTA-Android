<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/side_nav_bar"
    android:gravity="bottom"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"

    android:theme="@style/ThemeOverlay.AppCompat.Dark">

    <ImageView
        android:id="@+id/logo_imageview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/splashscreen_septalogowithtextandcopyright" />

    <TextView
        android:id="@+id/setpa_textview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/logo_imageview"
        android:layout_toEndOf="@+id/logo_imageview"
        android:layout_toRightOf="@+id/logo_imageview"
        android:paddingTop="10dp"
        android:text="@string/septa" />

    <TextView
        android:id="@+id/tagline_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/setpa_textview"
        android:layout_toEndOf="@+id/logo_imageview"
        android:layout_toRightOf="@+id/logo_imageview"
        android:text="@string/app_tag_line" />

</RelativeLayout>
